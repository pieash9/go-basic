package arrays

import "fmt"

func Arrays() {
	var arr [2]int

	fmt.Println(arr)
}

/*
	2. Phases:
		1. compilation phase (compile time)
		2. execution phase (run time)

  *************** compile phase ************
  ** code segment **
	main = func () {...}

*/
